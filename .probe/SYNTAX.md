# Mojo 1.0.0b3 cheat sheet for `json-mojo`

> **Version:** 1.3.0 | **Updated:** 2026-07-03

Ported from color-mojo's `.probe/SYNTAX.md` (2026-07-02, probed on `1.0.0b3.dev2026070123`); this workspace resolves `1.0.0b3.dev2026070206` — re-verify any line on first divergence. Two of the retired prototype's probe files were re-ported here (`probe_clmul.mojo`, `probe_mul128.mojo` — findings 18–19); its remaining probes (SIMD lanes, atof, bitcast, prefetch, string building) were not retained — re-derive by probing as the design needs them. Every line below is _probed_, not remembered.

## Language shape (ported, sibling-verified)

| Form                                   | Status                      | Use                                                                      |
| -------------------------------------- | --------------------------- | ------------------------------------------------------------------------ |
| `fn` keyword                           | REMOVED                     | use `def`                                                                |
| `alias` keyword                        | deprecated                  | use `comptime`                                                           |
| `@parameter if`                        | deprecated                  | use `comptime if`                                                        |
| `@register_passable("trivial")`        | REMOVED                     | conform to `TrivialRegisterPassable` trait                               |
| `Stringable` trait                     | gone                        | expose `name() -> StaticString` or `write_to[W: Writer](self, mut w: W)` |
| `EqualityComparable`                   | not in scope                | `Comparable` already implies it                                          |
| `owned` keyword in params              | replaced                    | `def f(var x: T)`                                                        |
| Module-level mutable `var`             | ERROR — globals unsupported | pass state down                                                          |
| Module-level `comptime` const          | OK                          | compile-time constants                                                   |
| Generic struct field referencing param | `Self.S`, not bare `S`      | `var sub: Self.S`                                                        |
| Trait method body                      | `...` (ellipsis)            | declaration only                                                         |
| Trait base for owned types             | needs `ImplicitlyDeletable` | `trait T(Copyable, Movable, ImplicitlyDeletable)`                        |
| `comptime if` inside `@always_inline`  | DCE verified                | erased branch produces no output                                         |
| `UnsafePointer` mutable origin         | `MutAnyOrigin`              | not `MutableAnyOrigin`                                                   |
| String byte slice                      | `s[byte=a:b]`               | direct `s[a:b]` rejected                                                 |

## Stdlib I/O (ported, sibling-verified)

| Need        | Import / call                                                   |
| ----------- | --------------------------------------------------------------- |
| Write to fd | `from std.io import FileDescriptor; FileDescriptor(2).write(s)` |
| Env var     | `from std.os import getenv` — `String`, empty if unset          |
| isatty      | `from std.os import isatty; isatty(2) -> Bool`                  |

## Ported findings (probed in color-mojo on 1.0.0b3.dev2026070123)

| #   | Finding                                                                                                                                                                                                                                    | Origin probe                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------- |
| 1   | `def emit[W: Writer](mut writer: W)` works as a generic bound; `String` conforms to `Writer`; variadic `writer.write("alpha ", 42)` works                                                                                                  | `probe_writer_trait.mojo`                 |
| 2   | `Writer` requires exactly one method: `write_string(mut self, string: StringSlice)` (declared in `std/format`); variadic `write(*args)` derived; **no `write_bytes` exists**; `List[UInt8]` does NOT conform; a 5-line wrapper struct does | `probe_custom_writer.mojo`                |
| 3   | `String(from_utf8=span)` raises (validating); `String(unsafe_from_utf8=span)` does not raise; `List[UInt8]` converts implicitly to `Span[UInt8]` at these call sites                                                                       | `probe_string_from_bytes.mojo`            |
| 4   | `String(unsafe_uninit_length=n)` allocates an exact-length string; `text.unsafe_ptr_mut()` exposes the buffer for direct byte fill                                                                                                         | `probe_string_uninitialized.mojo`         |
| 5   | Full `String.__init__` overload inventory: `capacity=`, `unsafe_from_utf8=`, `from_utf8=`, `from_utf8_lossy=`, `unsafe_uninit_length=`, `Writable` variadic, and more                                                                      | `probe_string_constructor_overloads.mojo` |
| 6   | Fluent immutable builder works: no-argument `__init__(out self)` plus chained methods; returning a local requires an explicit move (`return next^`) — implicit copy needs `ImplicitlyCopyable`                                             | `probe_fluent_construction.mojo`          |
| 7   | `case` is a reserved keyword — unusable as a parameter name                                                                                                                                                                                | `probe_function_as_parameter.mojo`        |
| 8   | NEGATIVE: a named `def` does not coerce to an anonymous `def () raises -> None` parameter type — test runners keep explicit try/except blocks                                                                                              | `probe_function_as_parameter.mojo`        |
| 9   | NEGATIVE: `InlineArray[T, n](v1, v2, …)` variadic-values constructor does not exist — use `uninitialized=True` plus stores, or a branch helper                                                                                             | quantize build                            |
| 10  | Passing `UnsafePointer` across a `MutAnyOrigin` parameter boundary warns (implicit origin erasure deprecated) — keep a concrete origin (pass `mut text: String`, take the pointer inside) or cast with `as_unsafe_any_origin()`            | decimal build                             |
| 11  | `mojo package` deprecated in favor of `mojo precompile`; `.mojopkg` in favor of `.mojoc`                                                                                                                                                   | packaging check                           |
| 12  | `StringSlice(unsafe_from_utf8=Span(inline_array)[0:n])` works — zero-allocation string view over stack storage                                                                                                                             | `probe_stringslice_from_stack.mojo`       |

## Local findings (probed here, 1.0.0b3.dev2026070206)

| #   | Finding                                                                                                                                                                                                                      | Probe file                     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| 13  | `std.reflection.reflect[T]` full field walk works: `is_struct()`, `field_count()`, `field_names()`, `field_types()`, `field_ref[i](instance)`; `comptime for` loops work; `conforms_to(T, Trait)` works (builtin, no import) | `probe_reflection.mojo`        |
| 14  | NEGATIVE settled: no foreign-trait retrofit exists — `conforms_to` returns False for non-declared types; the `to[T]` gateway matcher branches on `conforms_to` + type identity (`_type_is_eq` per the EmberJson evidence)    | `probe_reflection.mojo`        |
| 15  | Trait methods may carry default bodies (the ported sheet's "ellipsis only" is outdated); trait composition `comptime _Base = TraitA & TraitB` is used by working code                                                        | `probe_reflection.mojo`        |
| 16  | A `TrivialRegisterPassable` struct works as a comptime parameter and `comptime if` on its fields erases branches — the ParseOptions zero-cost mechanism, verified                                                            | `probe_options_parameter.mojo` |
| 17  | `String.capacity()` and `String.reserve(n)` exist and work — Document's 64-byte tail-padding guarantee is `reserve(byte_length + 64)` after the move-in                                                                      | `probe_string_capacity.mojo`   |
| 18  | PCLMULQDQ available via `llvm_intrinsic["llvm.x86.pclmulqdq", SIMD[DType.uint64, 2]]` — stage-1 string-state tracking on x86; portable prefix-XOR shift fallback remains the non-x86 path                                    | `probe_clmul.mojo` (re-port)   |
| 19  | `UInt128` exists with full widening multiply (`u64 × u64 → u128`, high/low extraction) — Eisel-Lemire unlocked; schoolbook fallback verified equivalent                                                                      | `probe_mul128.mojo` (re-port)  |

## Build-phase findings (compiler-taught, 1.0.0b3.dev2026070206)

Learned while building the library — each cost a compile error; every line was verified by the fix landing green. Source: the build itself (`json/value.mojo`, `json/serde.mojo`, `json/document.mojo`).

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Where proven                         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| 20  | `__extension Type(Trait):` EXISTS — retroactive trait conformance (OVERTURNS finding 14). Limits in findings 21–23                                                                                                                                                                                                                                                                                                                                                                                        | `value.mojo` String/Bool/SIMD        |
| 21  | An `__extension` must live in the TRAIT's defining module — a cross-module extension fails conformance with an unlocated "does not implement all requirements"                                                                                                                                                                                                                                                                                                                                            | container extensions saga            |
| 22  | A trait METHOD DEFAULT BODY breaks generic `__extension` conformance checks — keep protocol traits declaration-only (`...`) and route derivation through gateway functions (`to[T]` / `to_json_value`)                                                                                                                                                                                                                                                                                                    | `FromJson` / `ToJson`                |
| 23  | The ownership checker rejects extension bodies that ACCUMULATE a container across raising calls when elements are generic archetypes (List/Dict `from_json` impossible; Optional fine — nothing live across its raises). `comptime if conforms_to(...)` feeds the where-clause prover but NOT the ownership pass                                                                                                                                                                                          | container extensions saga            |
| 24  | `Dict.__setitem__` carries `where conforms_to(K/V, ImplicitlyDeletable)` — inside generic code, supply evidence via a `comptime if conforms_to(...)` branch                                                                                                                                                                                                                                                                                                                                               | Dict extension attempt               |
| 25  | A `where` clause on an implementing method breaks trait conformance; extension headers accept no `where` grammar at all                                                                                                                                                                                                                                                                                                                                                                                   | Dict extension attempt               |
| 26  | `type_of(trait_downcast[B](ref))` is B-typed and satisfies a matching trait bound — the dispatch idiom `x.to[type_of(field)]()`; `reflect[T].field_types()[i]` values are `AnyType`-typed (no attribute access — downcast-as-type to use)                                                                                                                                                                                                                                                                 | `default_from_json` walk             |
| 27  | `ImplicitlyDestructible` is a deprecated alias — use `ImplicitlyDeletable` (matches the toolkit standard)                                                                                                                                                                                                                                                                                                                                                                                                 | compiler warning                     |
| 28  | Out-parameter type does NOT drive overload selection — return-type-based dispatch is dead                                                                                                                                                                                                                                                                                                                                                                                                                 | session scratch probe (not retained) |
| 29  | `rebind_var[T](value^)` is the OWNED rebind; `rebind[T](x)` borrows (assigning it demands an explicit copy)                                                                                                                                                                                                                                                                                                                                                                                               | Dict key handling                    |
| 30  | Iterator protocol is Python-style: `def __iter__(self) -> Self` (with `.copy()` if not ImplicitlyCopyable) and `def __next__(mut self) raises StopIteration -> T`; `raise StopIteration()` ends iteration; `Sized` optional                                                                                                                                                                                                                                                                               | `_ElementIter` / `_MemberIter`       |
| 31  | `origin_of(x)` (NOT `__origin_of`); origins are FIELD-PRECISE — `origin_of(self._field)` ≠ `origin_of(self)`; widen with `rebind[Span[T, origin_of(self)]](...)`                                                                                                                                                                                                                                                                                                                                          | `Document.root`                      |
| 32  | `Int` is a SIMD-scalar alias — `__extension Int` is impossible ("can't find a struct named 'Int'"); one SIMD conformance with `Self.dtype` branches covers every width. `DType.index` is gone                                                                                                                                                                                                                                                                                                             | SIMD conformances                    |
| 33  | `_type_is_eq` was removed from `std.sys.intrinsics` (EmberJson's import is stale on this pin); dispatch via `conforms_to`, `reflect[T].base_name()`, or full-name matching                                                                                                                                                                                                                                                                                                                                | `to[T]` gateway                      |
| 34  | A parameter that determines the RETURN type must not be infer-only — `f[T: X, //]` cannot be bound explicitly at `f[Concrete]()`; the value lands on the next parameter                                                                                                                                                                                                                                                                                                                                   | `deserialize` signature              |
| 35  | `trait_downcast` is deprecated on this pin (warning: use `conforms_to(type_of(src), Trait)`). Verified replacement in the reflection walk: with `comptime assert conforms_to(field_types[i], Trait)` (or a `comptime if conforms_to(...)` branch) in scope, a PLAIN `ref field = r.field_ref[i](result)` binding carries the evidence — `type_of(field)` satisfies trait bounds and `field = {}` sees Defaultable. Supersedes the trait_downcast half of finding 26; the field walk compiles warning-free | `probe_downcast_migration.mojo`      |
| 36  | The container-deserialization ownership wall (finding 23) FELL: an `__extension` body MAY accumulate a container across raising calls when the raise path explicitly consumes the partial container (`result^.destroy_with(_dropper[Self.T])`, then `raise error^`) with comptime-assert element evidence — probed green for List AND Dict (incl. `Dict.__setitem__` where-evidence). Re-landing is BLOCKED by a compiler ICE on this pin: a cross-module `conforms_to(List[X], FromJson)` query crashes decl-body resolution of the extension even with a trivial body (the same query against `ToJson` works — serialization extensions are fine; `Optional(FromJson)` is fine). Minimal reproducer: import json, then `conforms_to(List[Int64], FromJson)`. Re-attempt each nightly | `probe_container_walls.mojo` |
| 37  | Mixed-type TUPLE RETURNS — `def f(...) -> (Int, UInt8)` — fail construction on this pin (`an element of 'args' ... does not conform to trait 'Movable'`); return one value and pass the rest as `mut` out-parameters (the json5 scanner's shape) | json5 build |
| 38  | `List[T](unsafe_uninit_length=n)` allocates with length set and elements uninitialized; `unsafe_ptr()` writes fill in place and `shrink(m)` truncates to the written prefix — the String-style exact-fill pattern, used by the tape builder's proven-bound unchecked appends | `probe_list_uninit.mojo` |
